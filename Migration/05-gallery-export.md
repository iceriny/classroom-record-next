 # 画廊与导出

 ## 画廊聚合逻辑

 - 按日期（YYYY-MM-DD）分组
 - 日期内按班级分组
 - 组内按 `createdAt` 倒序排列

 ## 多选行为

 - 进入多选模式后支持按“日期”或“班级”批量选中
 - 选中项可批量删除或导出 ZIP

 ## ZIP 导出

 - 生成 zip 文件到缓存目录
 - zip 内目录结构：`班级/日期/文件名`
 - 文件名来自原始 URI 的末尾部分
 - 路径字段需做非法字符清理
